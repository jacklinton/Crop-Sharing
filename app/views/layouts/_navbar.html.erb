    <div class="navbar-wrapper">
      <div class="container">

        <nav class="navbar navbar-inverse navbar-static-top">
          <div class="container">
            <div class="navbar-header">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
              <%= link_to "Crop Sharing", root_path, class: 'navbar-brand' %>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
              <ul class="nav navbar-nav">
                <li class="active"><%= link_to "Home", root_path %></li>
                <li><a href="#">Blog</a></li>
                <li><a href="#about">About Us</a></li>
                <li><a href="#contact">Contact Us</a></li>
                <li><a href="#help">Help</a></li>
              </ul>
              
              <ul class="nav navbar-nav navbar-right">
                <% if current_user %>
                  <li> 
                    <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
                  </li>
                <% else %>

                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">Sign Up<b class="caret"></b></a>
                  <ul class="dropdown-menu">
                   <li>
                    <div class="navbar-content">
                      <div class="row">
                        <div class="col-md-12">


                          <form class="form-signup">
                            <h3>Sign Up</h3>

                            <%= form_for @user, url: new_user_registration_path(@user) do |f| %>
                            <%= f.label :email, "Email address", class: 'sr-only' %>
                            <%= f.text_field :email, class: 'form-control', id: 'inputEmail', placeholder: 'Email address' %>
                            <%= f.label :password, class: 'sr-only' %>
                            <%= f.password_field :password, class: 'form-control', id: 'inputPassword', placeholder: 'Password' %>
                            <%= f.submit "Sign up", :class => 'btn btn-sm btn-primary' %>
                            <% end %>

                          </form>
                        </div>
                      </div>
                    </div>
                  </div>
                </li>
              </ul>
            </li>

            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Sign In<b class="caret"></b></a>
              <ul class="dropdown-menu">
               <li>
                <div class="navbar-content">
                  <div class="row">
                    <div class="col-md-12">
                      <form class="form-signin">
                        <h3>Sign In</h3>

                        <%= form_for @user, url: home_index_path(@home) do |f| %>
                        <%= f.label :email, "Email address", class: 'sr-only' %>
                        <%= f.text_field :email, class: 'form-control', id: 'inputEmail', placeholder: 'Email address' %>
                        <%= f.label :password, class: 'sr-only' %>
                        <%= f.password_field :password, class: 'form-control', id: 'inputPassword', placeholder: 'Password' %>
                        <%= f.submit "Sign in", :class => 'btn btn-sm btn-primary' %>
                        <% end %>
                        
                      </form>
                    </div>
                  </div>
                </div>
              </div>
            </li>
            <% end %>
          </ul>
        </li>

      </ul>
    </div>
  </div>
</nav>
</div>
</div>